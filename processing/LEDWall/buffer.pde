

PGraphics buffer;


void setupBuffer() {
  buffer = createGraphics(COLUMNS, ROWS, JAVA2D);
  //buffer.beginDraw();
  //buffer.background(255,0,0);
  //buffer.endDraw();
  println("BUFFER SETUP ...");
}
